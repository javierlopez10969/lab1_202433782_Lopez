;git
(def git)

;pull , recursion natural o de cola , siendo disntinto
;al de add ; Ej (pull zonas)
           ; ((git pull)zonas)

;add , añade los cambios locales al index
; salida : lista de nombre s de archivos 
